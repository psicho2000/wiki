Ggf. Gestenschalter > Gestenschalter > Benutzerdefiniert

Belegung der unteren Taste: Mausempfindlichkeit
    Master: maximal
    Benutzerdefiniert: 2. Stufe

Daumenradrichtung: Invertiert

AutoHotkey
    $WheelRight::FastForward()
    $WheelLeft::Rewind()

    FastForward() {
        WinGetActiveTitle, Title
        if (InStr(Title, "VLC media player")) {
            Send +{Right}
        } else if (InStr(Title, "Microsoft Edge") or InStr(Title, "Google Chrome")) {
            Send {Right}
        } else {
            Send {WheelRight}
        }
    }

    Rewind() {
        WinGetActiveTitle, Title
        if (InStr(Title, "VLC media player")) {
            Send +{Left}
        } else if (InStr(Title, "Microsoft Edge") or InStr(Title, "Google Chrome")) {
            Send {Left}
        } else {
            Send {WheelLeft}
        }  
    }